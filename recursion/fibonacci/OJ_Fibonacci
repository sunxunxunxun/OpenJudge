#include <iostream>
#include <cstdio>

using namespace std;

int fibonacci(int x){
    if(x==1 || x== 2) return 1;
    else {
        return fibonacci(x-1) +fibonacci(x-2);
    }
}
int main()
{
    int T; cin >> T;
    while(T--){
        int n; cin >> n;
        cout << fibonacci(n) << "\n";
    }
    return 0;
}
